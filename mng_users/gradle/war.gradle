apply plugin: "war"

bootWar {
    mainClass = "managerusers.myapp.MngUsersApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
