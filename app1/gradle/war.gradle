apply plugin: "war"

bootWar {
    mainClass = "com.manager_users.ManagerUsersApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
